# thebeerbrother

Blog de peliculas construido en Jekyll

## Levantar en local

1. Clonar el repositorio
2. Instalar las dependencias con `bundle install`
3. Ejecutar el servidor con `bundle exec jekyll serve`
4. Abrir el navegador en `http://localhost:4000`

## Deploy
